{
  "name": "spendshield",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "contracts",
    "backend",
    "frontend"
  ],
  "scripts": {
    "build": "npm run build -w contracts && npm run build -w backend && npm run build -w frontend",
    "dev": "npm run dev -w backend",
    "lint": "npm run lint -w contracts && npm run lint -w backend && npm run lint -w frontend",
    "test": "npm run test -w contracts"
  }
}
